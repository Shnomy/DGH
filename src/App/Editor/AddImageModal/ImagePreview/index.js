import React from 'react';
import {connect} from 'cerebral/react';
import {ImagePreviewWrapper} from './elements';

export default connect({

}, function ImagePreview () {
  return (
      <ImagePreviewWrapper>

      </ImagePreviewWrapper>
  );
});
