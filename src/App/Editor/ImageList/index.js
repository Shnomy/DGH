import React from 'react';
import {connect} from 'cerebral/react';
import {ImageListWrapper} from './elements';

export default connect({

}, function ImageList() {
  return (
    <ImageListWrapper>

    </ImageListWrapper>
  );
});
